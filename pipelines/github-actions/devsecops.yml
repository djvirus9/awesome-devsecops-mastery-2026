name: DevSecOps reference checks

# Keep the repository's shared files and canonical workflow when copying this adapter.
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  reference:
    uses: ./.github/workflows/devsecops-golden-pipeline.yml
